.row
  .col-xs-12
    Search Results For:
    = link_to click_tag_path, class: 'tag-link' do
      %span.label.label-primary.search-term Eric
    
    = link_to click_tag_path, class: 'tag-link' do
      %span.label.label-primary.search-term Soccer

%hr.search-divider

- ['photos/soccer1.jpg', 'photos/soccer2.jpg', 'photos/soccer3.jpg', 'photos/soccer4.jpg'].each_slice(3) do |imgs|
  .row
    - imgs.each do |img|
      .col-xs-4
        = image_tag img, class: 'img-thumbnail img-selectable'

= render 'toolbar'